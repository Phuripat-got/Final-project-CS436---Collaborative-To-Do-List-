# 📄 เอกสารขอบเขตของระบบ (System Scope Document)
**Project:** Real-time Multi-user To-Do List Application

## 1. 🌐 ภาพรวมของระบบ (System Overview)
ระบบนี้คือเว็บแอปพลิเคชัน **รายการสิ่งที่ต้องทำ (To-Do List)** ที่อนุญาตให้ผู้ใช้งานหลายคน (**Multi-user**) เข้ามาจัดการรายการงานร่วมกันได้ในเวลาเดียวกัน (**Real-time**) โดยข้อมูลจะถูกบันทึกไว้อย่างถาวร ไม่สูญหายเมื่อรีเฟรชหน้าจอ

### 👥 ผู้ใช้งานระบบ (Actors)
เนื่องจากเป็นการจำลองสถานการณ์ (Simulated Scenario) จะแบ่งกลุ่มผู้ใช้งานดังนี้:

* **User A (Creator):** ผู้เริ่มสร้าง Task
* **User B (Collaborator):** ผู้ที่เข้ามาเห็น Task และทำการแก้ไขหรือลบ
    > *Note:* ในทางเทคนิคสำหรับเวอร์ชัน MVP นี้ ทุกคนมีสิทธิ์เท่าเทียมกัน แต่ระบบจะมีการระบุตัวตนเพื่อแยกแยะว่าใครเป็นผู้กระทำรายการนั้นๆ

---

## 2. 📋 ความต้องการด้านฟังก์ชัน (Functional Requirements - FR)
ส่วนนี้ระบุสิ่งที่ระบบ **"ต้องทำได้"** เพื่อให้บรรลุวัตถุประสงค์:

### **FR-01: การจัดการงาน (Task Management)**
* **Create:** ผู้ใช้สามารถสร้างงานใหม่โดยระบุชื่อหัวข้อ (Title) ระบบจะบันทึก ID ของผู้สร้าง (Creator ID) โดยอัตโนมัติ
* **Read:** แสดงรายการงานทั้งหมดแบบ Real-time ประกอบด้วย: ชื่อ, สถานะ, และผู้สร้าง
* **Update:**
    * *Edit Text:* แก้ไขข้อความชื่อของงานได้
    * *Toggle Status:* เปลี่ยนสถานะไปมาระหว่าง "รอดำเนินการ" (Pending) และ "เสร็จสิ้น" (Completed)
* **Delete:** ลบงานออกจากรายการได้

### **FR-02: การทำงานร่วมกันแบบ Real-time**
* **Instant Sync:** เมื่อ User A เพิ่มหรือแก้ไขงาน หน้าจอของ User B ต้องอัปเดตทันทีโดยไม่ต้องกด Refresh
* **Status Update:** เมื่อ User B ทำเครื่องหมายว่าเสร็จ หน้าจอของ User A ต้องแสดงการขีดฆ่ารายการนั้นทันที

### **FR-03: การจำลองผู้ใช้ (User Simulation)**
* **Identification:** ระบบต้องมีกลไกในการระบุตัวตนอย่างง่าย (เช่น การกรอกชื่อเล่นก่อนเข้าใช้งาน หรือการสุ่ม User ID เก็บไว้ใน Session) เพื่อนำไปแสดงผลในฟิลด์ `createdBy`

---

## 3. 🚀 ความต้องการที่ไม่ใช่ฟังก์ชัน (Non-Functional Requirements - NFR)
ส่วนนี้ระบุ **"ประสิทธิภาพและคุณภาพ"** ของระบบ:

* **💾 Data Persistence:** ข้อมูลต้องไม่หายเมื่อปิด Browser (ใช้ Database สำหรับข้อมูลกลาง และ Local Storage สำหรับ User ID ชั่วคราว)
* **⚡ Latency:** ความล่าช้าในการส่งข้อมูลระหว่าง User A และ B ควรต่ำมาก (Low Latency) เพื่อให้ผู้ใช้รู้สึกถึงความ Real-time อย่างแท้จริง
* **🔄 Concurrency:** ระบบต้องรองรับกรณีที่มีการแก้ไขงานเดียวกันพร้อมกัน (ในระดับ MVP ใช้หลักการ **Last Write Wins** หรือผู้ที่บันทึกทีหลังเป็นฝ่ายชนะ)

---

## 4. 📊 การวิเคราะห์ข้อมูล (Data Analysis & Schema)
ออกแบบโครงสร้างข้อมูล (Data Model) เพื่อรองรับ Persistence และ Multi-user

**โครงสร้าง Object ของ "Task" (JSON Format):**

```json
{
  "id": "uuid-v4-string",          // รหัสอ้างอิงงาน (Primary Key)
  "title": "ซื้อกาแฟเข้าออฟฟิศ",      // ชื่องาน
  "isCompleted": false,            // สถานะ (เสร็จ = true / ไม่เสร็จ = false)
  "createdBy": "User_JohnDoe",     // ชื่อหรือ ID ของผู้สร้าง
  "createdAt": 1701234567890       // Timestamp (เพื่อใช้ในการเรียงลำดับ)
}
